Project Report
---
title: "Biol 4315 Lab-1Report"
output: html_document
date: "2026-09-10"
---
Partner: Catherine Ho

## Loading rmarkdown package
library(rmarkdown)
## Loading knitr package
library(knitr)

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

## Installing vegan package
```{r, eval = FALSE}
install.packages("vegan")
```


## ggplot2
Load ggplot2 package and plot a scatter plot of speed vs dist from the cars dataset
```{r}
library(ggplot2)
ggplot(cars, aes(x = speed, y = dist)) +
       geom_point() +
       labs(title = "Scatter Plot of speed vs dist", x = "speed", y = "dist")
```


## Annotation via Funannotate
Retrieve Funannotate from Docker Hub
```{bash, eval = FALSE}
docker pull nextgenusfs/funannotate
```


## Sequali installation
Code to install sequali
```{bash,  eval = FALSE}
conda create -n sequali-env python=3.13 conda-forge
conda activate sequali-env
conda install sequali=1.0.2 -c bioconda
```

